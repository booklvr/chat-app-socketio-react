{
  "name": "backend",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "dependencies": {
    "colors": "^1.4.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "socket.io": "^4.1.3"
  },
  "scripts": {
    "server": "nodemon",
    "client": "yarn start --prefix ../client",
    "dev": "concurrently \"yarn run server\" \"yarn run client\""
  },
  "devDependencies": {
    "concurrently": "^6.2.0"
  }
}
